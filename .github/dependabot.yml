# Dependabot keeps the Go module current. The lockfiles under
# internal/testutil/testdata_multi are parser fixtures and list only
# current, advisory-free versions, so nothing there needs watching.
version: 2
updates:
  - package-ecosystem: gomod
    directory: "/"
    schedule:
      interval: weekly
    groups:
      go-modules:
        patterns: ["*"]
