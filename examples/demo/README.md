# Attack demo

Two small repositories reproduce a tag hijack on real GitHub objects and
show SCG catching it: the action under `action/`, the consumer under
`repo/`. Each README says how to set it up; nothing here runs against a
real third-party action.
