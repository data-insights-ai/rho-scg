# SCG CLI — Distribution Task List

## Install Script — DONE

- [x] `install.sh` — detect OS/arch, download binary, verify checksum
- [x] Hosted at scg.bds421.com/install.sh (HTTPS)
- [x] Installs to ~/.local/bin (no sudo)
- [x] Cross-compiled binaries: linux/darwin amd64/arm64
- [x] SHA256 checksums

## GitHub Action Wrapper — DONE (in repo)

- [x] `action.yml` — composite action with check, scope, init, audit modes
- [x] All flags: step-name, lockfile, sanitize, no-verify, json, version

## Release Binaries — PARTIAL

- [x] `.goreleaser.yml` configuration
- [x] Cross-compiled binaries uploaded to scg.bds421.com/releases/
- [ ] GitLab CI release pipeline (tag → build → upload → update latest)
- [ ] Automated — currently manual scp upload
- [ ] Homebrew tap formula
- [ ] Docker image

## Remaining

- [ ] CI pipeline: auto-build + upload on tag push (eliminate manual scp)
- [ ] GitHub Action published to GitHub Marketplace
- [ ] `scg self-update` command (optional convenience)
